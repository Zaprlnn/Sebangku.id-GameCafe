import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; import WebSocket from 'ws'; global.WebSocket = WebSocket; const env = fs.readFileSync('frontend/.env', 'utf-8').split('\n').reduce((acc, line) => { const match = line.match(/^([^=]+)=(.*)$/); if (match) acc[match[1].trim()] = match[2].trim(); return acc; }, {}); const supabase = createClient(env['VITE_SUPABASE_URL'], env['VITE_SUPABASE_ANON_KEY']); async function run() { const { data } = await supabase.from('customer_profiles').select('*'); console.log(data); } run();
